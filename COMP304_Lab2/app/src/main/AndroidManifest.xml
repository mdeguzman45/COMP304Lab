<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcdeguzman_comp304sec002_lab2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Marcdeguzman_COMP304Sec002_Lab2">
        <activity android:name=".OrderConfirmation"></activity>
        <activity android:name=".CustomerInformation" />
        <activity android:name=".Payment" />
        <activity android:name=".Checkout" />
        <activity android:name=".Pizza_Setup" />
        <activity android:name=".Pizza_Selection" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>